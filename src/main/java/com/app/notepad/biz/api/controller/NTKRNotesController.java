package com.app.notepad.biz.api.controller;

import com.app.notepad.common.service.ServiceResponse;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


/**
 * @author navneet on 06/19/2022
 */

@RestController
@RequestMapping("ntkr")
public interface NTKRNotesController {

    /**Todo:
     * 1. /getAllNotesForUserID  - for that need to formulate the datatype of what this endpoint will return and create a DTO for it
     * 2.
      */


}
